# ============================================================
# Aplikacja do Przetwarzania Obrazów – pełna wersja (9 zakładek)
# Autorzy: Magdalena Czyżewska 21227, Adrian Witów 21319
# ============================================================

# ── WYMAGANE ──────────────────────────────────────────────
Pillow>=10.0.0
opencv-python>=4.8.0
numpy>=1.24.0

# ── PyTorch (zakładka F + opcjonalnie znaki drogowe) ──────
# macOS Apple Silicon:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
# macOS Intel / Linux CPU:
#   pip install torch torchvision
# Windows GPU CUDA 12.1:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0

# ── MediaPipe 0.10+ (zakładka Szkielet + Mimika) ─────────
# WAŻNE: wersja 0.10+ używa nowego Tasks API (nie mp.solutions)
# Model (~30 MB) pobierany automatycznie przy pierwszym użyciu
mediapipe>=0.10.0

# ── DeepFace (zakładka Mimika twarzy) ─────────────────────
deepface>=0.0.79
tf-keras>=2.15.0

# ── YOLOv8 (zakładka Sieć neuronowa – detekcja) ──────────
ultralytics>=8.0.0

# ── Testy ─────────────────────────────────────────────────
pytest>=7.0.0
